apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "bt-otel-collector-helm.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "bt-otel-collector-helm.labels" . | nindent 4 }}
data:
  otel-collector-config.yaml: |
{{ tpl (toYaml .Values.config) . | indent 4 }}
